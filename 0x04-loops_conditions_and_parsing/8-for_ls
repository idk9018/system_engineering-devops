#!/usr/bin/env bash
# This script lists files in current directory, showing only part after first dash

for file in *
do
    # Only process regular files (skip directories)
    if [ -f "$file" ]; then
        # Display only the part after the first dash
        echo "${file#*-}"
    fi
done
